interface:
  display_name: "T3 Code Sync"
  short_description: "Merge upstream into SIGIDI with boundary proof"
  default_prompt: "Use $t3code-sync to safely merge the latest upstream T3 Code main branch into the detected SIGIDI default branch, preserve active worktrees, and prove downstream and migration compatibility."
